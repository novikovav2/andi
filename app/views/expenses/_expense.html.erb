<div id="<%= dom_id(expense) %>">
  <div class="expense-card">
    <%= link_to edit_event_expense_path(expense.event, expense),
                data: { turbo_frame: "modal" },
                class: "expense-card-link" do %>
      <div class="expense-card-row">
        <div class="expense-left">
          <div class="expense-icon">
            <%= expense_icon(expense) %>
          </div>

          <div class="expense-info">
            <div class="expense-title">
              <%= expense.title %>
            </div>

            <div class="expense-meta">
              Оплатил <%= expense.payer.name %>
            </div>

            <% if expense.weighted_split? %>
              <div class="expense-meta-secondary">
                По количеству: <%= expense.split_weight_labels.join(" / ") %>
              </div>
            <% else %>
              <div class="expense-meta-secondary">
                Разделили между: <%= expense.participants.count %>
              </div>
            <% end %>
          </div>
        </div>

        <div class="expense-right">
          <div class="expense-amount">
            <%= rubles(expense.amount_cents) %>
          </div>

          <div class="expense-chevron">
            ›
          </div>
        </div>
      </div>

    <% end %>

    <% if receipt_scan_image_available?(expense.receipt_scan) %>
      <%= link_to "📄 Чек",
                  receipt_scan_image_path(expense.receipt_scan),
                  target: "_blank",
                  rel: "noopener",
                  class: "expense-receipt-link" %>
    <% end %>
  </div>
</div>
